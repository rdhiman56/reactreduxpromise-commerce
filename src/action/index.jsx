import {LOGIN} from '../constants';

export function getLogin() {
    const url = '';
    //const query = graphqlQuery_getNavCategories;
        return {
            type {
                type:LOGIN,
                //payload: graphql_call(url, query)
            };
        }
}